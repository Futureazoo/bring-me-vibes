import React from 'react';
import './Mixer.css';

// Contains all of the mixers for the current scene

function Mixer() {
  return (  
    <div>
      <h1>Mixer</h1>
    </div>
  );
}

export default Mixer;